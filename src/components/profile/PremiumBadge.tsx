
import { Badge } from "@/components/ui/badge";

const PremiumBadge = () => (
  <Badge variant="default" className="bg-gradient-to-r from-yellow-400 to-orange-500 text-white px-3 py-1 font-bold rounded-lg">
    Premium
  </Badge>
);

export default PremiumBadge;
